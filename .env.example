# JWT Secret Key (generate with: openssl rand -hex 32)
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7

# Twilio Configuration for WhatsApp
TWILIO_ACCOUNT_SID=your_twilio_account_sid_here
TWILIO_AUTH_TOKEN=your_twilio_auth_token_here
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Optional: Override default Twilio sandbox number with your own WhatsApp Business number
# TWILIO_WHATSAPP_NUMBER=whatsapp:+1234567890

# SMTP Configuration for Email Invitations
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password_here
SMTP_USE_TLS=True

# Web Domain for Email Links
WEBDOMAIN=localhost:3000

# JWT Token Configuration (Optional)
# Access token expiration in minutes (default: 1440 = 24 hours)
# ACCESS_TOKEN_EXPIRE_MINUTES=1440
# Refresh token expiration in days (default: 30 days)
# REFRESH_TOKEN_EXPIRE_DAYS=30

# Expo Token
EXPO_TOKEN=your_expo_token_here

# Storage Secret for File Upload
STORAGE_SECRET=your_storage_secret_here

# Google Services JSON for Firebase (Mobile App)
# Path to google-services.json file for Android Firebase configuration
# Default: ./google-services.json
GOOGLE_SERVICES_JSON=./google-services.json

# External AI Service Configuration
# Note: Service URLs and access tokens are managed via the database
# using the /api/admin/service-tokens API endpoint

# WhatsApp Template Configuration
WHATSAPP_CONFIRMATION_TEMPLATE_SID=your_whatsapp_confirmation_template_sid_here
WHATSAPP_RECONNECTION_TEMPLATE_SID=your_whatsapp_reconnection_template_sid_here

# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here

# Redis Configuration (for Celery broker and result backend)
# These values are used to construct Celery broker/backend URLs automatically
# For Kubernetes: point to your Redis service (e.g., redis-service.namespace.svc.cluster.local)
# For Docker: use service name (redis)
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0

# Celery Configuration
# Broker and backend URLs are auto-constructed from REDIS_* variables above
# Format: redis://{REDIS_HOST}:{REDIS_PORT}/{REDIS_DB}
# Optional overrides (uncomment if you need custom URLs):
# CELERY_BROKER_URL=redis://redis:6379/0
# CELERY_RESULT_BACKEND=redis://redis:6379/0

# Celery Broadcast Settings
# Maximum recipients per batch when sending broadcasts (default: 50)
# BROADCAST_BATCH_SIZE=50
# Retry intervals in minutes for failed broadcasts (default: 5, 15, 60)
# BROADCAST_RETRY_INTERVALS=5,15,60
# Button payload for broadcast confirmation (default: read_broadcast)
# BROADCAST_CONFIRMATION_BUTTON_PAYLOAD=read_broadcast

# Optional: Flower Monitoring UI (Celery task monitoring)
# Username and password for Flower web interface
# FLOWER_USER=admin
# FLOWER_PASSWORD=admin123
# FLOWER_PORT=5555